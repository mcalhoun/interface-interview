/**
 * The SurfaceAdapter service and its Playwright implementation. Observation is the
 * accessibility tree only: no method returns markup and none accepts a selector.
 * See docs/adr/0001-accessibility-tree-is-the-only-observation-channel.md.
 *
 * Scaffold only. Filled in by a later ticket.
 */

export {}
